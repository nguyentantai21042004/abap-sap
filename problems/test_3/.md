## Round 3 Bugs — 6/6 FIXED

1. ~~Nút Upload Excel ngay tại trang Project list bị dump khi chạy~~
   **FIXED** — Ẩn nút UPLOAD + DN_TMPL cho tất cả roles (CODE_PBO `status_0400`)

2. ~~Không lưu được note khi tạo bug mới hay cả chỉnh sửa note~~
   **FIXED** — Thêm sy-subrc check sau SAVE_TEXT (CODE_F02) + cần tạo Text Object ZBUG trong SE75

3. ~~Lỗi filter Project, sau khi di chuyển qua nhiều trang thì quay lại hiện full~~
   **FIXED** — Chỉ load data lần đầu, quay lại giữ nguyên filter (CODE_PBO `init_project_list`)

4. ~~Nút Delete User yêu cầu chọn user nhưng không thể chọn được~~
   **FIXED** — Xóa guard check `gv_tc_user_selected` (CODE_F01) + sửa Flow Logic Screen 0500 trong SE51 (xóa `ON CHAIN-REQUEST`)

5. ~~Description và note bị giới hạn ký tự~~
   **FIXED** — Liên quan Bug 2, text object ZBUG chưa tồn tại nên text mất khi reload. SE75 config xong là xong.

6. ~~3 nút upload chỉ có 1 nút đầu chạy, 2 nút sau bị dump~~
   **FIXED** — Ẩn UP_REP + UP_FIX cho tất cả roles/modes (CODE_PBO `status_0300`) + fix `lv_fname_only(100)` dump (CODE_F01)

### Deploy checklist:
- [x] Code fixes written (CODE_PBO, CODE_F01, CODE_F02)
- [ ] SE75: Tạo text object ZBUG + Text IDs Z001/Z002/Z003
- [ ] SE51: Screen 0500 — xóa `ON CHAIN-REQUEST` trong PAI Flow Logic
- [ ] SE38: Paste 3 includes + activate
- [ ] Test 6 scenarios

Chi tiết deploy: xem `ROUND3_FIX_GUIDE.md`
